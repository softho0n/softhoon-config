spring:
  datasource:
    uri: helloworld 
    username: root
    password: password
  cloud:
    gateway:
      routes:
        - id: user-service
          uri: https://www.naver.com
          predicates:
            - Path=/login
            - Method=POST
